services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: linkhop
      POSTGRES_PASSWORD: linkhop
      POSTGRES_DB: linkhop
    ports: ["5432:5432"]

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
